{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "cheap.doc/mcp",
  "description": "Passport, ID and MRZ recognition through the doc.cheap API — $0.01 per document",
  "version": "0.3.0",
  "websiteUrl": "https://doc.cheap/docs/guides/use-the-mcp-server",
  "repository": {
    "url": "https://gitlab.com/doccheap/ocr-mcp",
    "source": "gitlab"
  },
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@doc-cheap/mcp",
      "version": "0.3.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "DOC_CHEAP_API_KEY",
          "description": "Your doc.cheap API key. Left unset, the server calls the public sandbox key, which runs 10 free recognitions and has no balance.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "DOC_CHEAP_API_BASE",
          "description": "Base URL of the doc.cheap API. Only set this to reach a different deployment.",
          "isRequired": false,
          "isSecret": false,
          "default": "https://api.doc.cheap"
        }
      ]
    }
  ],
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://mcp.doc.cheap/mcp",
      "headers": [
        {
          "name": "Authorization",
          "description": "Your doc.cheap API key as `Bearer <key>`. Left out, the server uses the public sandbox key, which runs 10 free recognitions and has no balance.",
          "value": "Bearer {api_key}",
          "isRequired": false,
          "isSecret": true,
          "variables": {
            "api_key": {
              "description": "Your doc.cheap API key.",
              "isRequired": false,
              "isSecret": true
            }
          }
        }
      ]
    }
  ]
}
